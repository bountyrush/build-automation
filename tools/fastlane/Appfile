# iOS
app_identifier(ENV["IOS_BUNDLE_IDENTIFIER"] || ENV["BUNDLE_IDENTIFIER"]) # The bundle identifier of your app
itc_team_id(ENV["IOS_ITC_TEAM_ID"]) # App Store Connect Team ID
team_id(ENV["IOS_TEAM_ID"]) # Developer Portal Team ID
apple_id(ENV["IOS_APP_STORE_USER_NAME"]) #App store connect user name for iOS


# Android
json_key_file(ENV["ROOT_FOLDER_PATH"] + '/' + ENV["GOOGLE_PLAY_SERVICE_ACCOUNT_JSON"]) # Path to the json secret file - Follow https://docs.fastlane.tools/actions/supply/#setup to get one
json_key = json_key_file
package_name(ENV["ANDROID_BUNDLE_IDENTIFIER"] || ENV["BUNDLE_IDENTIFIER"])
#developer_account_id(ENV["GOOGLE_PLAY_DEVELOPER_ACCOUNT_ID"])




